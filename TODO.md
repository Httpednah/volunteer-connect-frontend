# Fix Build Issue - TODO List

## Steps to fix the nested project structure:

- [x] 1. Move `index.html` from nested directory to root
- [x] 2. Move `vite.config.js` from nested directory to root
- [x] 3. Move `eslint.config.js` from nested directory to root
- [x] 4. Move `README.md` from nested directory to root
- [x] 5. Move `public/` directory from nested directory to root
- [x] 6. Move `src/` directory from nested directory to root (merge/overwrite)
- [x] 7. Copy complete `package.json` from nested directory to root
- [x] 8. Remove the nested `volunteer-connect-frontend/` directory
- [x] 9. Run `npm install`
- [x] 10. Run `npm run build` to verify the fix ✅
